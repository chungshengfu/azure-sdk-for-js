/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import { RaiPolicy } from "../operationsInterfaces";
import * as coreClient from "@azure/core-client";
import * as Mappers from "../models/mappers";
import * as Parameters from "../models/parameters";
import { AzureMachineLearningServices } from "../azureMachineLearningServices";
import {
  SimplePollerLike,
  OperationState,
  createHttpPoller,
} from "@azure/core-lro";
import { createLroSpec } from "../lroImpl";
import {
  RaiPolicyDeleteOptionalParams,
  RaiPolicyDeleteResponse,
  RaiPolicyGetOptionalParams,
  RaiPolicyGetResponse,
  RaiPolicyPropertiesBasicResource,
  RaiPolicyCreateOptionalParams,
  RaiPolicyCreateResponse,
} from "../models";

/** Class containing RaiPolicy operations. */
export class RaiPolicyImpl implements RaiPolicy {
  private readonly client: AzureMachineLearningServices;

  /**
   * Initialize a new instance of the class RaiPolicy class.
   * @param client Reference to the service client
   */
  constructor(client: AzureMachineLearningServices) {
    this.client = client;
  }

  /**
   * Deletes the specified Content Filters associated with the Azure OpenAI account.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workspaceName Azure Machine Learning Workspace Name
   * @param endpointName Name of the endpoint resource.
   * @param raiPolicyName Name of the Rai Policy.
   * @param options The options parameters.
   */
  async beginDelete(
    resourceGroupName: string,
    workspaceName: string,
    endpointName: string,
    raiPolicyName: string,
    options?: RaiPolicyDeleteOptionalParams,
  ): Promise<
    SimplePollerLike<
      OperationState<RaiPolicyDeleteResponse>,
      RaiPolicyDeleteResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<RaiPolicyDeleteResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        resourceGroupName,
        workspaceName,
        endpointName,
        raiPolicyName,
        options,
      },
      spec: deleteOperationSpec,
    });
    const poller = await createHttpPoller<
      RaiPolicyDeleteResponse,
      OperationState<RaiPolicyDeleteResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Deletes the specified Content Filters associated with the Azure OpenAI account.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workspaceName Azure Machine Learning Workspace Name
   * @param endpointName Name of the endpoint resource.
   * @param raiPolicyName Name of the Rai Policy.
   * @param options The options parameters.
   */
  async beginDeleteAndWait(
    resourceGroupName: string,
    workspaceName: string,
    endpointName: string,
    raiPolicyName: string,
    options?: RaiPolicyDeleteOptionalParams,
  ): Promise<RaiPolicyDeleteResponse> {
    const poller = await this.beginDelete(
      resourceGroupName,
      workspaceName,
      endpointName,
      raiPolicyName,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * Gets the specified Content Filters associated with the Azure OpenAI account.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workspaceName Azure Machine Learning Workspace Name
   * @param endpointName Name of the endpoint resource.
   * @param raiPolicyName Name of the Rai Policy.
   * @param options The options parameters.
   */
  get(
    resourceGroupName: string,
    workspaceName: string,
    endpointName: string,
    raiPolicyName: string,
    options?: RaiPolicyGetOptionalParams,
  ): Promise<RaiPolicyGetResponse> {
    return this.client.sendOperationRequest(
      {
        resourceGroupName,
        workspaceName,
        endpointName,
        raiPolicyName,
        options,
      },
      getOperationSpec,
    );
  }

  /**
   * Update the state of specified Content Filters associated with the Azure OpenAI account.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workspaceName Azure Machine Learning Workspace Name
   * @param endpointName Name of the endpoint resource.
   * @param raiPolicyName Name of the Rai Policy.
   * @param body Azure OpenAI Content Filters resource.
   * @param options The options parameters.
   */
  async beginCreate(
    resourceGroupName: string,
    workspaceName: string,
    endpointName: string,
    raiPolicyName: string,
    body: RaiPolicyPropertiesBasicResource,
    options?: RaiPolicyCreateOptionalParams,
  ): Promise<
    SimplePollerLike<
      OperationState<RaiPolicyCreateResponse>,
      RaiPolicyCreateResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<RaiPolicyCreateResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        resourceGroupName,
        workspaceName,
        endpointName,
        raiPolicyName,
        body,
        options,
      },
      spec: createOperationSpec,
    });
    const poller = await createHttpPoller<
      RaiPolicyCreateResponse,
      OperationState<RaiPolicyCreateResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Update the state of specified Content Filters associated with the Azure OpenAI account.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workspaceName Azure Machine Learning Workspace Name
   * @param endpointName Name of the endpoint resource.
   * @param raiPolicyName Name of the Rai Policy.
   * @param body Azure OpenAI Content Filters resource.
   * @param options The options parameters.
   */
  async beginCreateAndWait(
    resourceGroupName: string,
    workspaceName: string,
    endpointName: string,
    raiPolicyName: string,
    body: RaiPolicyPropertiesBasicResource,
    options?: RaiPolicyCreateOptionalParams,
  ): Promise<RaiPolicyCreateResponse> {
    const poller = await this.beginCreate(
      resourceGroupName,
      workspaceName,
      endpointName,
      raiPolicyName,
      body,
      options,
    );
    return poller.pollUntilDone();
  }
}
// Operation Specifications
const serializer = coreClient.createSerializer(Mappers, /* isXml */ false);

const deleteOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/endpoints/{endpointName}/raiPolicies/{raiPolicyName}",
  httpMethod: "DELETE",
  responses: {
    200: {
      headersMapper: Mappers.RaiPolicyDeleteHeaders,
    },
    201: {
      headersMapper: Mappers.RaiPolicyDeleteHeaders,
    },
    202: {
      headersMapper: Mappers.RaiPolicyDeleteHeaders,
    },
    204: {
      headersMapper: Mappers.RaiPolicyDeleteHeaders,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.workspaceName,
    Parameters.raiPolicyName,
    Parameters.endpointName2,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const getOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/endpoints/{endpointName}/raiPolicies/{raiPolicyName}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.RaiPolicyPropertiesBasicResource,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.workspaceName,
    Parameters.raiPolicyName,
    Parameters.endpointName2,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const createOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/endpoints/{endpointName}/raiPolicies/{raiPolicyName}",
  httpMethod: "PUT",
  responses: {
    200: {
      bodyMapper: Mappers.RaiPolicyPropertiesBasicResource,
    },
    201: {
      bodyMapper: Mappers.RaiPolicyPropertiesBasicResource,
    },
    202: {
      bodyMapper: Mappers.RaiPolicyPropertiesBasicResource,
    },
    204: {
      bodyMapper: Mappers.RaiPolicyPropertiesBasicResource,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  requestBody: Parameters.body53,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.workspaceName,
    Parameters.raiPolicyName,
    Parameters.endpointName2,
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer,
};
